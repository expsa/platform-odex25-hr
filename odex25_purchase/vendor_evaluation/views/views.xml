<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="vendor_evaluation.list">
      <field name="name">vendor_evaluation list</field>
      <field name="model">vendor_evaluation.vendor_evaluation</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="vendor_evaluation.action_window">
      <field name="name">vendor_evaluation window</field>
      <field name="res_model">vendor_evaluation.vendor_evaluation</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="vendor_evaluation.action_server">
      <field name="name">vendor_evaluation server</field>
      <field name="model_id" ref="model_vendor_evaluation_vendor_evaluation"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="vendor_evaluation" id="vendor_evaluation.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="vendor_evaluation.menu_1" parent="vendor_evaluation.menu_root"/>
    <menuitem name="Menu 2" id="vendor_evaluation.menu_2" parent="vendor_evaluation.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="vendor_evaluation.menu_1_list" parent="vendor_evaluation.menu_1"
              action="vendor_evaluation.action_window"/>
    <menuitem name="Server to list" id="vendor_evaluation" parent="vendor_evaluation.menu_2"
              action="vendor_evaluation.action_server"/>
    -->
  </data>
</odoo>